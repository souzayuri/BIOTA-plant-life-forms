citation(vegan)
library("vegan")
citation("vegan")
citation("np")
library("np")
setwd("C:/Users/Yuri/Google Drive/Yuri/Mestrado/parcelas_biota/projeto/PPG/tese/analises/01_dados/heatmap")
library(gplots)
library(vegan)
#Carregando os dados
heatplant <- read.table("data_plant_life_heatmap_ordered_d17_04_2019.txt", header = TRUE, sep = "\t", row.names = 1)
str(heatplant)
heatplant  <- log10(heatplant +1)
#Transformando os dados em matriz para a função heatmap.2
heatplant <- as.matrix(heatplant)
head(heatplant )
heatmap(heatplant)
heatmap(heatplant, scale="column")
heatmap(heatplant)
heatmap(heatplant, scale="column")
my_palette <- colorRampPalette(c("darkred", "yellow", "green", "blue"))(n =399)
heatmap.2(heatplant, trace= "none", density.info = "none", col=my_palette,
RowSideColors = c(rep("gray", 43),
rep("black", 43)), cexRow = .5)
heatmap.2(heatplant, cellnote = heatplant, trace= "none", density.info = "none", col=my_palette,
RowSideColors = c(rep("gray", 43),
rep("black", 43)), cexRow = .5)
heatmap.2(heatplant, trace= "none", density.info = "none", col=my_palette,
RowSideColors = c(rep("gray", 43),
rep("black", 43)), cexRow = .5)
heatmap.2(heatplant, trace= "none", density.info = "none", col=my_palette,
RowSideColors = c(rep("gray", 43),
rep("black", 43)), cexRow = .7)
heatmap.2(heatplant, trace= "none", density.info = "none", col=my_palette,
RowSideColors = c(rep("gray", 43),
rep("black", 43)), cexRow = .5)
heatmap.2(heatplant, trace= "none", density.info = "none", col=my_palette, breaks=col_breaks,
RowSideColors = c(rep("gray", 43),
rep("black", 43)), cexRow = .5)
heatmap.2(heatplant, trace= "none", density.info = "none", col=my_palette,
RowSideColors = c(rep("gray", 43),
rep("black", 43)), cexRow = .5)
heatmap.2(heatplant, trace= "none", density.info = "none", col=my_palette, dendrogram="row"
RowSideColors = c(rep("gray", 43),
rep("black", 43)), cexRow = .5)
heatmap.2(heatplant, trace= "none", density.info = "none", col=my_palette, dendrogram="row",
RowSideColors = c(rep("gray", 43),
rep("black", 43)), cexRow = .5)
heatmap.2(heatplant, trace= "none", density.info = "none", col=my_palette,
RowSideColors = c(rep("gray", 43),
rep("black", 43)), cexRow = .5)
heatmap.2(heatplant, trace= "none", density.info = "none", col=my_palette,
RowSideColors = c(rep("gray", 43), rep("black", 43)), cexRow = .5)
par(lend = 1)           # square line ends for the color legend
legend("topright",      # location of the legend on the heatmap plot
legend = c("Open", "Closed"), # category labels
col = c("gray", "black"),  # color key
lty= 1,             # line style
lwd = 10            # line width
)
legend("topright",      # location of the legend on the heatmap plot
legend = c("Open", "Closed"), # category labels
col = c("gray", "black"),  # color key
lty= 2,             # line style
lwd = 10            # line width
)
par(lend = 1)           # square line ends for the color legend
legend("topright",      # location of the legend on the heatmap plot
legend = c("Open", "Closed"), # category labels
col = c("gray", "black"),  # color key
lty= 2,             # line style
lwd = 10            # line width
)
legend("toleft",      # location of the legend on the heatmap plot
legend = c("Open", "Closed"), # category labels
col = c("gray", "black"),  # color key
lty= 1,             # line style
lwd = 10            # line width
)
legend("tolaft",      # location of the legend on the heatmap plot
legend = c("Open", "Closed"), # category labels
col = c("gray", "black"),  # color key
lty= 1,             # line style
lwd = 10            # line width
)
heatmap.2(heatplant, density.info = "none", col=my_palette,
RowSideColors = c(rep("gray", 43), rep("black", 43)), cexRow = .5)
heatmap.2(heatplant, trace= "none", density.info = "none", col=my_palette,
RowSideColors = c(rep("gray", 43), rep("black", 43)), cexRow = .5)
par(lend = 1)           # square line ends for the color legend
legend("topright",      # location of the legend on the heatmap plot
legend = c("Open", "Closed"), # category labels
col = c("gray", "black"),  # color key
lty= 1,             # line style
lwd = 10            # line width
)
legend("top",      # location of the legend on the heatmap plot
legend = c("Open", "Closed"), # category labels
col = c("gray", "black"),  # color key
lty= 1,             # line style
lwd = 10            # line width
)
legend("belowright",      # location of the legend on the heatmap plot
legend = c("Open", "Closed"), # category labels
col = c("gray", "black"),  # color key
lty= 1,             # line style
lwd = 10            # line width
)
legend("downright",      # location of the legend on the heatmap plot
legend = c("Open", "Closed"), # category labels
col = c("gray", "black"),  # color key
lty= 1,             # line style
lwd = 10            # line width
)
legend("bottom",      # location of the legend on the heatmap plot
legend = c("Open", "Closed"), # category labels
col = c("gray", "black"),  # color key
lty= 1,             # line style
lwd = 10            # line width
)
par(lend = 1)           # square line ends for the color legend
legend("bottom",      # location of the legend on the heatmap plot
legend = c("Open", "Closed"), # category labels
col = c("gray", "black"),  # color key
lty= 1,             # line style
lwd = 10            # line width
)
heatmap.2(heatplant, trace= "none", density.info = "none", col=my_palette,
RowSideColors = c(rep("gray", 43), rep("black", 43)), cexRow = .5)
par(lend = 1)           # square line ends for the color legend
legend("bottom",      # location of the legend on the heatmap plot
legend = c("Open", "Closed"), # category labels
col = c("gray", "black"),  # color key
lty= 1,             # line style
lwd = 10            # line width
)
draw(lgd, x = unit(0.5, "npc"), y = unit(0.5, "npc"))
pushViewport(viewport(width = 0.9, height = 0.9))
